Yocto Notes
-------------

To search for a recipe:
    bitbake-layers show-recipes <recipe>

To search for a variable in a recipe:
    bitbake -e <recipe> | grep <variable>
example:
    bitbake -e gdcfonts | grep ^WORKDIR=

To clear a recipe:
    bitbake -c cleanall <recipe>
example:
    bitbake -c cleanall gdcfonts

To bake only one recipe:
    bitbake <recipe>
exampe:
    bitbake gdcfonts


uboot
-------

printenv - to print uboot variables

for LEC_imX6:
remove jumper on B.SEL0
Press space bar when prompted, before uboot tries to load from emmc or flash.
change to
boot_sd=LOAD='fatload mmc 1:1'; BOOTARGS='mmcb1k1p2 ro '; run boot
then
run boot_sd


tools for deploying image
--------------------------

balena etcher - burn an image to usb or sd card

rufus - burn an image to a usb or sd card

dd

bmaptool - linux tool to burn an image to an sd card

